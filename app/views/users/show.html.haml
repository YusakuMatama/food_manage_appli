= render "layouts/header"
.main_container
  %h1.text-center.p-3.mb-2.bg-primary.text-white.w-75.p-3
    カロリー消費量
  .d-flex.flex-row.bd-highlight.mb-3
    .p-2.bd-highlight
      .card{style: "width: 30rem;"}
        .card-body.p-3.mb-2.bg-info.text-white
          %h4.card-title 
            =@user.name
            さんのデータ
          %ul.list-unstyled
            %li<>
            本日の摂取カロリー : 
            %span{style: "text-decoration: underline;"} 
              = @total_calorie
              kcal
            %li<> 
            1日の推定消費カロリー   : 
            %span{style: "text-decoration: underline;"} 
              = @user.est_energy_req
              kcal
            %br
            - if (@user.est_energy_req - @total_calorie >= 0)
              %li<>
              本日はあと
              %span{style: "text-decoration: underline; color: yellow; font-weight: bold;"}
                = @user.est_energy_req - @total_calorie
              kcalまでにおさえましょう。
            - else
              %li<>
              %span{style: "text-decoration: underline; color: red; font-weight: bold;"} 
                = @total_calorie - @user.est_energy_req
              %span{style: "font-weight: bold;"}
                kcal超過しています。本日はもう食べないほうがいいです。
    .p-2.bd-highlight
      .card{style: "width: 30rem;"}
        .card-body.p-3.mb-2.bg-info.text-white
          - unless @user.metabolism_id == 1
            %h4.card-title 
              = @metabolism_under.age_base
              から 
              = @metabolism_top.age_base
              才までの平均データ
          - else
            %h4.card-title 
              = @metabolism_top.age_base
              才以下の平均データ
          %ul.list-unstyled
            %li 
              平均体重          : 
              = @metabolism_top.base_weight
              kg
            %li 
              推定消費カロリー    : 
              = @user.est_energy_req
              kcal
            %li
              基礎代謝          : 
              = @metabolism_top.base_metabolic_rate
              kcal
            %br
            %li 推定消費カロリーは基礎代謝×1.75(一般的な活動レベルの指標)で計算しています。
  %h1.text-center.p-3.mb-2.bg-primary.text-white.w-75.p-3
    本日の食事
  .d-flex.flex-row.bd-highlight.mb-3
    .p-2.bd-highlight
      .card{style: "width: 30rem;"}
        .card-body.p-3.mb-2.bg-success.text-white
          %h4.card-title.text-center 本日
          %ul.list-unstyled
            - @user_eat_data_today.each do |eat_data|
              %li
              = eat_data.created_at.hour
              時
              = eat_data.created_at.min
              分：
              = eat_data.food.name
              = eat_data.food.calorie 
              kcal
          = link_to "昨日の食事", "/", class: "card-link"
          = link_to "今週の食事", "/", class: "card-link"
          = link_to "今月の食事", "/", class: "card-link"
